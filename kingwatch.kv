#:import ArcGauge ui.widgets.ArcGauge
#:import StatCard ui.widgets.StatCard
#:import dp kivy.metrics.dp

# ── StatCard ───────────────────────────────────────────────────────────────
<StatCard>:
    orientation: "vertical"
    size_hint_y: None
    height: self.minimum_height
    padding: [dp(12), dp(10), dp(12), dp(10)]
    spacing: dp(4)

    canvas.before:
        Color:
            rgba: self.bg_color
        RoundedRectangle:
            pos: self.pos
            size: self.size
            radius: [dp(14)]

    Label:
        size_hint_y: None
        height: dp(18)
        text: root.title.upper()
        font_size: dp(10)
        bold: True
        color: root.title_color
        halign: "left"
        valign: "middle"
        text_size: self.size

    BoxLayout:
        size_hint_y: None
        height: dp(100)
        spacing: dp(12)

        ArcGauge:
            id: gauge
            size_hint_x: None
            width: dp(100)
            size_hint_y: 1
            pct:         root.bar_pct
            arc_color:   root.bar_color
            arc_bg:      root.bar_bg
            label_text:  root.value
            label_color: root.value_color
            thickness:   dp(9)

        BoxLayout:
            orientation: "vertical"
            spacing: dp(3)
            padding: [0, dp(8), 0, dp(8)]

            Label:
                text: root.subtitle
                font_size: dp(12)
                color: root.sub_color
                halign: "left"
                valign: "middle"
                text_size: self.size
                size_hint_y: 1

            Label:
                size_hint_y: None
                height: dp(15) if root.detail1 else 0
                opacity: 1 if root.detail1 else 0
                text: root.detail1
                font_size: dp(10)
                color: root.detail_color
                halign: "left"
                valign: "middle"
                text_size: self.size

            Label:
                size_hint_y: None
                height: dp(15) if root.detail2 else 0
                opacity: 1 if root.detail2 else 0
                text: root.detail2
                font_size: dp(10)
                color: root.detail_color
                halign: "left"
                valign: "middle"
                text_size: self.size


# ── RootWidget ─────────────────────────────────────────────────────────────
<RootWidget>:
    orientation: "vertical"

    canvas.before:
        Color:
            rgba: root.bg
        Rectangle:
            pos:  self.pos
            size: self.size

    # Header bar
    BoxLayout:
        size_hint_y: None
        height: dp(50)
        padding: [dp(14), dp(7)]
        spacing: dp(8)

        canvas.before:
            Color:
                rgba: root.card_bg
            Rectangle:
                pos:  self.pos
                size: self.size

        # App title
        Label:
            text: "KingWatch Pro"
            font_size: dp(14)
            bold: True
            color: root.text_col
            halign: "left"
            valign: "middle"
            text_size: self.size
            size_hint_x: None
            width: dp(130)

        # Live clock - replaces Hide button
        Label:
            id: clock_label
            text: root.clock_str
            font_size: dp(18)
            bold: True
            color: root.accent
            halign: "center"
            valign: "middle"
            text_size: self.size
            size_hint_x: 1

        # Theme button
        Button:
            text: "Theme"
            size_hint: None, None
            size: dp(64), dp(36)
            font_size: dp(12)
            background_color: root.accent
            color: 0, 0, 0, 1
            on_release: root.cycle_theme()

        # Collapse toggle (small icon-style)
        Button:
            text: "^" if root.collapsed else "v"
            size_hint: None, None
            size: dp(36), dp(36)
            font_size: dp(16)
            bold: True
            background_color: root.dim_col
            color: root.text_col
            on_release: root.toggle_collapse()

    # Scrollable cards
    ScrollView:
        opacity:     0 if root.collapsed else 1
        size_hint_y: 0 if root.collapsed else 1
        do_scroll_x: False

        GridLayout:
            cols: 1
            size_hint_y: None
            height: self.minimum_height
            spacing: dp(10)
            padding: [dp(10), dp(10), dp(10), dp(10)]

            StatCard:
                id: fps_card
                title:        "FPS / DISPLAY"
                bg_color:     root.card_bg
                title_color:  root.dim_col
                bar_color:    root.accent
                bar_bg:       root.bar_bg
                value_color:  root.accent
                sub_color:    root.sub_col
                detail_color: root.dim_col

            StatCard:
                id: cpu_card
                title:        "CPU"
                bg_color:     root.card_bg
                title_color:  root.dim_col
                bar_color:    root.accent
                bar_bg:       root.bar_bg
                value_color:  root.accent
                sub_color:    root.sub_col
                detail_color: root.dim_col

            StatCard:
                id: ram_card
                title:        "RAM"
                bg_color:     root.card_bg
                title_color:  root.dim_col
                bar_color:    root.accent
                bar_bg:       root.bar_bg
                value_color:  root.accent
                sub_color:    root.sub_col
                detail_color: root.dim_col

            StatCard:
                id: battery_card
                title:        "BATTERY"
                bg_color:     root.card_bg
                title_color:  root.dim_col
                bar_color:    root.accent
                bar_bg:       root.bar_bg
                value_color:  root.accent
                sub_color:    root.sub_col
                detail_color: root.dim_col

            StatCard:
                id: network_card
                title:        "NETWORK"
                bg_color:     root.card_bg
                title_color:  root.dim_col
                bar_color:    root.accent
                bar_bg:       root.bar_bg
                value_color:  root.accent
                sub_color:    root.sub_col
                detail_color: root.dim_col

            StatCard:
                id: storage_card
                title:        "STORAGE"
                bg_color:     root.card_bg
                title_color:  root.dim_col
                bar_color:    root.warn
                bar_bg:       root.bar_bg
                value_color:  root.warn
                sub_color:    root.sub_col
                detail_color: root.dim_col

            StatCard:
                id: thermal_card
                title:        "THERMAL"
                bg_color:     root.card_bg
                title_color:  root.dim_col
                bar_color:    root.danger
                bar_bg:       root.bar_bg
                value_color:  root.danger
                sub_color:    root.sub_col
                detail_color: root.dim_col